import styled from '@emotion/styled';
import { Box } from '@mui/system';

export const AuthWrapper = styled(Box)(
  () => `
    overflow: auto;
    flex: 1;
    overflow-x: hidden;
    align-items: center;
`
);
